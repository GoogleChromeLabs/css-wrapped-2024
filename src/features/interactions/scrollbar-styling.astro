---
import FeatureLayout from "./../_layout.astro";
import Title from "../../components/feature.title.astro";
import Author from "../../components/author.astro";
import Video from "../../components/video.astro";
import CodePen from "../../components/codepen.astro";
import { Code } from "astro:components";
import { Icon } from "astro-icon/components";

import slugify from "./../../util/slugify";

export const title = "Custom Scrollbars";
const slug = slugify(title);
---

<FeatureLayout slug={slug}>
  <Title slug={slug} title={title} />

  <div class="support">
    <baseline-status featureId="scrollbar-color"></baseline-status>
    <baseline-status featureId="scrollbar-width"></baseline-status>
  </div>

  <p>
    Styling scrollbars has long been possible with the <code
      >::-webkit-scrollbar-*</code
    > pseudo-elements. This approach works fine in Chrome and Safari, but was never
    standardized by the CSS Working Group.
  </p>

  <p>
    Available from Chrome 121 are the <code>scrollbar-width</code> and <code
      >scrollbar-color</code
    > properties to style the width and, respectively, the color of the scrollbar.
  </p>

  <Code
    lang="css"
    theme="css-variables"
    code={`.scroller {
  scrollbar-color: hotpink blue;
  scrollbar-width: 10%;
}`}
  />

  <p>
    These properties are also supported by Firefox and have partial support in
    Safari.
  </p>

  <figure>
    <CodePen id="web-dot-dev/yLwMexO" height={500} />
    <figcaption>
      Custom Scrollbars demo, with a fallback to <code
        >::-webkit-scrollbar-*</code
      >
    </figcaption>
  </figure>

  <section class="resources">
    <h4>Resources</h4>
    <ul>
      <li>
        <a
          href="https://developer.mozilla.org/en-US/docs/Web/CSS/scrollbar-color"
          ><code>scrollbar-color</code> on MDN</a
        >
      </li>
      <li>
        <a
          href="https://developer.mozilla.org/en-US/docs/Web/CSS/scrollbar-width"
          ><code>scrollbar-width</code> on MDN</a
        >
      </li>
    </ul>
  </section>

  <Author name="bramus" />

  <footer>
    <a href="https://codepen.io/web-dot-dev/pen/yLwMexO">Try demo</a>
    <div class="articles">
      <a href="https://developer.chrome.com/docs/css-ui/scrollbar-styling"
        >Article on developer.chrome</a
      >
    </div>
  </footer>
</FeatureLayout>
